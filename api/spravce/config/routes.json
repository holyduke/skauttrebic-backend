{
  "routes": [
    {
      "method": "GET",
      "path": "/spravces",
      "handler": "spravce.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/spravces/count",
      "handler": "spravce.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/spravces/:id",
      "handler": "spravce.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/spravces",
      "handler": "spravce.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/spravces/:id",
      "handler": "spravce.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/spravces/:id",
      "handler": "spravce.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
